// Empty stub for Node core modules that some npm packages (like `xlsx`) reference
// behind a `typeof require !== 'undefined'` check that never actually runs on
// React Native, but that Metro still needs to resolve when bundling. See
// metro.config.js.
module.exports = {};
